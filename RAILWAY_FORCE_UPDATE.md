# 🚂 Принудительное обновление на Railway

## Проблема
Railway иногда не обновляет код автоматически после push в GitHub. Это может быть связано с:
- Кэшированием старого кода
- Проблемами с webhook от GitHub
- Задержками в синхронизации

## Решения

### 1. Принудительный редеплой через Railway Dashboard
1. Зайдите в Railway Dashboard
2. Выберите ваш проект
3. Перейдите в настройки сервиса (Settings)
4. Найдите раздел "Deployments" или "Redeploy"
5. Нажмите "Redeploy" или "Deploy Latest Commit"

### 2. Принудительный редеплой через Railway CLI
```bash
# Установите Railway CLI (если еще не установлен)
npm i -g @railway/cli

# Войдите в Railway
railway login

# Выберите проект
railway link

# Принудительно задеплойте
railway up
```

### 3. Проверка текущего коммита на Railway
1. В Railway Dashboard откройте ваш сервис
2. Перейдите в раздел "Deployments"
3. Проверьте, какой коммит сейчас задеплоен
4. Сравните с последним коммитом в GitHub

### 4. Создание нового коммита для триггера
Если Railway не обновляется, можно создать пустой коммит:
```bash
git commit --allow-empty -m "Trigger Railway redeploy"
git push origin v4.0
```

### 5. Проверка логов на Railway
После редеплоя проверьте логи:
- Должно появиться сообщение: "Using custom creator function to filter sslmode from connect args"
- Если этого сообщения нет, значит Railway использует старый код

### 6. Полный редеплой через Railway Dashboard
1. Зайдите в Railway Dashboard
2. Выберите ваш проект
3. Перейдите в "Settings" → "Danger Zone"
4. Нажмите "Redeploy" или удалите и создайте сервис заново

## Текущая проблема с sslmode

Проблема: `asyncpg` не поддерживает параметр `sslmode` в URL или connect_args.

Решение: Используется кастомная функция `creator`, которая фильтрует `sslmode` перед вызовом `asyncpg.connect`.

Если проблема сохраняется после обновления:
1. Проверьте, что в логах есть сообщение "Using custom creator function..."
2. Если сообщения нет, Railway использует старый код
3. Принудительно обновите через один из методов выше

