# 📋 SPIN Training Bot v4.0 — Сводка проекта

> Полная сводка проекта для обсуждения с нейросетью

---

## 🎯 Что это за проект?

**SPIN Training Bot** — Telegram-бот для обучения технике продаж SPIN (Situation, Problem, Implication, Need-payoff). 

Бот проводит интерактивные тренировки, где пользователь играет роль продавца, а бот — клиента. Бот анализирует ответы пользователя, дает обратную связь и генерирует отчеты.

**Версия 4.0** — это полный рефакторинг с добавлением:
- ✅ Модульной системы оплаты и подписок
- ✅ PostgreSQL базы данных (multi-tenant архитектура)
- ✅ Системы промокодов
- ✅ Админ-панели
- ✅ Системы достижений (badges)

---

## 🏗️ Архитектура проекта

### Технологический стек

- **Python 3.11+**
- **python-telegram-bot** (20.x) — Telegram Bot API
- **SQLAlchemy 2.0** (async) — ORM для работы с БД
- **PostgreSQL** / **SQLite** — база данных
- **OpenAI API** — для анализа ответов и генерации кейсов
- **asyncio** — асинхронная архитектура

### Структура проекта

```
SPIN Training BOT Final/
├── bot.py                          # Главный файл бота
├── config.py                       # Конфигурация
│
├── database/                       # База данных
│   ├── models.py                   # SQLAlchemy модели (User, Subscription, Payment, Promocode, etc.)
│   └── database.py                 # Подключение к БД (async)
│
├── services/                       # Бизнес-логика
│   ├── user_service.py             # Управление пользователями (v3 - in-memory)
│   ├── user_service_db.py          # Управление пользователями (v4 - PostgreSQL)
│   ├── llm_service.py              # Интеграция с OpenAI
│   ├── training_service.py         # Логика тренировок SPIN
│   └── achievement_service.py       # Система достижений
│
├── engine/                         # Движок тренировок
│   ├── scenario_loader.py          # Загрузка сценариев
│   ├── case_generator.py            # Генерация кейсов
│   ├── question_analyzer.py        # Анализ ответов пользователя
│   └── report_generator.py         # Генерация отчетов
│
├── modules/                        # Модули
│   ├── payments/                   # 🎯 МОДУЛЬ ОПЛАТЫ (v4.0)
│   │   ├── config.py               # Тарифы и настройки
│   │   ├── handlers.py             # Telegram обработчики
│   │   ├── admin_handlers.py       # Админ-панель (промокоды, статистика)
│   │   ├── subscription.py         # Логика подписок
│   │   ├── promocodes.py           # Система промокодов
│   │   ├── keyboards.py            # Клавиатуры
│   │   ├── messages.py             # Тексты воронки продаж
│   │   └── providers/              # Платежные провайдеры (YooKassa, CloudPayments, Prodamus)
│   │
│   └── active_listening.py         # Детектор активного слушания
│
├── scenarios/                      # Сценарии тренировок
│   └── spin_sales/                 # Сценарий SPIN продаж
│       └── config.json
│
└── infrastructure/
    └── health_server.py             # Health check сервер
```

---

## ✅ Что реализовано

### 1. Основной функционал бота (v3.0)

- ✅ **Тренировки SPIN** — интерактивные диалоги с клиентом
- ✅ **Анализ ответов** — LLM анализирует технику SPIN пользователя
- ✅ **Генерация отчетов** — детальные отчеты после тренировки
- ✅ **Система достижений** — badges за прогресс
- ✅ **Активное слушание** — бонусы за уточняющие вопросы
- ✅ **Множественные сценарии** — загрузка из JSON

### 2. Модуль оплаты (v4.0)

- ✅ **Система подписок**
  - Месячная подписка (990₽)
  - Годовая подписка (6990₽)
  - Кредитная модель (pay-per-use)
  
- ✅ **Промокоды**
  - 3 типа: тренировки, бесплатный месяц, кредиты
  - Лимиты использования
  - Срок действия
  - **Админ-панель** — пошаговое создание через Telegram

- ✅ **Бесплатный доступ**
  - 2 тренировки за подписку на канал
  - Проверка через Bot API

- ✅ **Воронка продаж**
  - 8-этапная воронка с конверсионными текстами
  - Социальное доказательство
  - Гарантия возврата

- ✅ **Платежные провайдеры** (заглушки готовы)
  - YooKassa
  - CloudPayments
  - Prodamus

### 3. База данных (v4.0)

- ✅ **Модели:**
  - `User` — пользователи (telegram_id, статистика)
  - `Subscription` — подписки (тип, срок, кредиты)
  - `Payment` — платежи (сумма, провайдер, статус)
  - `Promocode` — промокоды
  - `PromocodeUsage` — использование промокодов
  - `FreeTraining` — бесплатные тренировки
  - `BotSession` — сессии ботов (multi-tenant)
  - `UserAchievement` — достижения пользователей

- ✅ **Multi-tenant архитектура**
  - Поддержка нескольких ботов на одной БД
  - Изоляция данных по `bot_name`

### 4. Админ-панель (v4.0)

- ✅ **Команда `/admin`**
  - Управление промокодами
  - Просмотр статистики
  - Пошаговое создание промокодов через диалог

---

## 🔄 Миграция v3 → v4

### Что изменилось:

1. **Хранилище данных:**
   - v3: `dict` в памяти (`user_data = {}`)
   - v4: PostgreSQL через SQLAlchemy

2. **Сервисы:**
   - v3: `UserService` (in-memory)
   - v4: `UserServiceDB` (PostgreSQL)

3. **Новые модули:**
   - `modules/payments/` — система оплаты
   - `database/` — модели и подключение

4. **Интеграция:**
   - Добавлен `@subscription_required` декоратор
   - Добавлены handlers для оплаты
   - Добавлена админ-панель

---

## 📊 Текущее состояние

### ✅ Работает

- Основной функционал тренировок
- Система подписок (проверка доступа)
- Промокоды (создание, активация)
- Админ-панель (создание промокодов через диалог)
- База данных (PostgreSQL)
- Система достижений
- Multi-tenant архитектура

### ⏳ В разработке / TODO

- **Платежные провайдеры** — заглушки готовы, нужна реализация API
- **Вебхуки** — для автоматической активации подписок после оплаты
- **Автопродление** — cron-задача для проверки истекающих подписок
- **Миграция данных v3→v4** — скрипт для переноса пользователей

---

## 🚀 Как запустить

### 1. Установка зависимостей

```bash
pip install -r REQUIREMENTS_v4.txt
```

### 2. Настройка окружения

```bash
cp .env.v4.example .env
# Заполнить:
# - BOT_TOKEN
# - OPENAI_API_KEY
# - DATABASE_URL (PostgreSQL или SQLite)
```

### 3. Инициализация БД

```bash
python -c "import asyncio; from database import init_db; asyncio.run(init_db())"
```

### 4. Запуск

```bash
python bot.py
```

---

## 🎯 Ключевые файлы для обсуждения

### Основные

- `bot.py` — главный файл, координация всех сервисов
- `config.py` — конфигурация (токены, пути)
- `database/models.py` — модели БД
- `services/training_service.py` — логика тренировок

### Модуль оплаты

- `modules/payments/handlers.py` — обработчики команд оплаты
- `modules/payments/admin_handlers.py` — админ-панель
- `modules/payments/subscription.py` — логика подписок
- `modules/payments/promocodes.py` — система промокодов

### Движок

- `engine/question_analyzer.py` — анализ ответов пользователя
- `engine/report_generator.py` — генерация отчетов
- `engine/case_generator.py` — генерация кейсов

---

## 💡 Особенности реализации

### 1. Асинхронная архитектура

Все операции с БД и API — асинхронные (`async/await`). Используется `asyncio` и `asyncpg` для PostgreSQL.

### 2. Декоратор `@subscription_required`

Автоматически проверяет доступ и показывает меню оплаты, если доступа нет:

```python
from modules.payments import subscription_required

@subscription_required
async def start_training(update, context):
    # Код тренировки
    pass
```

### 3. Multi-tenant архитектура

Все модели имеют поле `bot_name` для изоляции данных между ботами:

```python
user = User(telegram_id=123, bot_name="spin_bot")
```

### 4. ConversationHandler для админ-панели

Пошаговое создание промокодов через диалог в Telegram:

```
1. Ввод кода
2. Выбор типа
3. Ввод значения
4. Ввод лимита использований
5. Ввод срока действия
```

---

## 🔧 Конфигурация

### Тарифы

`modules/payments/config.py`:
```python
TARIFFS = {
    'month': {'price': 990, 'duration_days': 30},
    'year': {'price': 6990, 'duration_days': 365}
}
```

### Модель монетизации

```python
MONETIZATION_MODEL = 'subscription'  # или 'credits'
```

---

## 📝 Команды бота

### Пользовательские

- `/start` — начать тренировку (требует подписку)
- `/payment` или `/buy` — меню оплаты
- `/stats` — статистика
- `/rank` — достижения

### Админские

- `/admin` — админ-панель
  - Создание промокодов
  - Просмотр статистики
  - Управление пользователями

---

## 🐛 Известные проблемы / TODO

1. **Платежные провайдеры** — нужна реализация API методов
2. **Вебхуки** — для автоматической активации подписок
3. **Автопродление** — cron для проверки истекающих подписок
4. **Миграция v3→v4** — скрипт для переноса данных

---

## 📚 Документация

- `README_v4.md` — полная документация модуля оплаты
- `V4_INTEGRATION_GUIDE.md` — руководство по интеграции
- `V4_SUMMARY.md` — краткая сводка v4.0
- `PROJECT_STRUCTURE_v4.txt` — структура файлов

---

## 🎯 Для обсуждения с нейросетью

### Вопросы, которые можно задать:

1. **Архитектура:**
   - Как улучшить multi-tenant изоляцию?
   - Оптимизация запросов к БД?
   - Паттерны для обработки ошибок?

2. **Модуль оплаты:**
   - Реализация платежных провайдеров?
   - Оптимизация воронки продаж?
   - A/B тестирование?

3. **Производительность:**
   - Кэширование запросов?
   - Оптимизация LLM запросов?
   - Масштабирование на много пользователей?

4. **Безопасность:**
   - Валидация платежей?
   - Защита от спама?
   - Rate limiting?

---

## 📊 Статистика проекта

- **Файлов:** ~50+
- **Строк кода:** ~5000+
- **Моделей БД:** 8
- **Telegram handlers:** 20+
- **Платежных провайдеров:** 3 (заглушки)

---

**Последнее обновление:** 2025-01-XX  
**Версия:** 4.0  
**Статус:** ✅ Работает, готов к продакшн (после реализации платежных провайдеров)

