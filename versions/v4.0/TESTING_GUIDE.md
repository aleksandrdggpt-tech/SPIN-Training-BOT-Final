# 🧪 Руководство по тестированию бота v4.0

## 📋 Быстрый старт

### 1. Подготовка окружения

```bash
# Перейти в папку v4.0
cd versions/v4.0

# Создать виртуальное окружение (если еще не создано)
python3 -m venv venv

# Активировать виртуальное окружение
source venv/bin/activate  # для macOS/Linux
# или
venv\Scripts\activate  # для Windows

# Установить зависимости
pip install -r requirements.txt
```

### 2. Настройка переменных окружения

```bash
# Скопировать пример конфигурации
cp env.v4.example .env

# Отредактировать .env файл и добавить:
# - BOT_TOKEN - токен вашего Telegram бота
# - OPENAI_API_KEY - ключ OpenAI API (опционально)
# - ANTHROPIC_API_KEY - ключ Anthropic API (опционально)
# - DATABASE_URL - URL базы данных (опционально, по умолчанию SQLite)
```

### 3. Запуск бота

```bash
# Простой запуск
python3 bot.py

# Или с логированием в файл
python3 bot.py > bot.log 2>&1

# Или в фоновом режиме (macOS/Linux)
nohup python3 bot.py > bot.log 2>&1 &
```

### 4. Проверка работоспособности

#### 4.1. Проверка импортов
```bash
python3 -c "import sys; sys.path.insert(0, '.'); from bot import *; print('✅ Все импорты работают')"
```

#### 4.2. Запуск автоматических тестов
```bash
python3 test_functionality.py
```

#### 4.3. Проверка health check
После запуска бота:
```bash
curl http://localhost:8080/health
# Должен вернуть: {"status": "ok"}
```

---

## 🧪 Тестирование функциональности

### 1. Тест основных команд

В Telegram боте проверить:

1. **`/start`** - должно показать приветственное сообщение
   - ✅ Приветствие отображается
   - ✅ Нет ошибок в консоли

2. **`/help`** - должно показать справку
   - ✅ Список команд отображается
   - ✅ Команды payment закомментированы в справке

3. **`/stats`** - должно показать статистику
   - ✅ Статистика отображается (даже если пустая)

4. **`/rank`** - должно показать ранг
   - ✅ Ранг отображается

5. **`/case`** - должно показать информацию о кейсе
   - ✅ Информация отображается (или сообщение, что кейс не начат)

### 2. Тест тренировки

1. **Начало тренировки:**
   - Написать "начать" или "старт"
   - ✅ Должен сгенерироваться кейс
   - ✅ Кейс должен содержать описание клиента

2. **Задание вопроса:**
   - Задать вопрос клиенту (например: "Расскажите о вашей компании")
   - ✅ Должен прийти ответ от клиента
   - ✅ В логах должна быть классификация вопроса (S/P/I/N)

3. **Получение обратной связи:**
   - Написать "ДА" после вопроса
   - ✅ Должна прийти обратная связь от наставника

4. **Завершение тренировки:**
   - Написать "завершить" или задать 10 вопросов
   - ✅ Должен сгенерироваться финальный отчет
   - ✅ Отчет должен содержать статистику по типам вопросов

### 3. Тест базы данных

```bash
# Проверить, что БД создается
ls -la *.db  # для SQLite
# или проверить подключение к PostgreSQL
```

### 4. Проверка логов

```bash
# Просмотр логов в реальном времени
tail -f bot.log

# Поиск ошибок
grep -i error bot.log

# Поиск предупреждений
grep -i warning bot.log
```

---

## 🔍 Проверка закомментированного функционала

### Убедиться, что payment функционал закомментирован:

```bash
# Проверить, что нет активных импортов payment
grep -n "from modules.payments" bot.py | grep -v "^#"

# Должно быть пусто (все импорты закомментированы)

# Проверить, что нет активных вызовов payment функций
grep -n "register_payment_handlers\|get_payment_menu_keyboard\|get_start_training_keyboard" bot.py | grep -v "^#"

# Должно быть пусто (все вызовы закомментированы)
```

---

## ⚠️ Возможные проблемы

### 1. Ошибка импорта модулей

**Проблема:** `ModuleNotFoundError: No module named '...'`

**Решение:**
```bash
# Убедиться, что все зависимости установлены
pip install -r requirements.txt

# Проверить, что виртуальное окружение активировано
which python3  # должен указывать на venv/bin/python3
```

### 2. Ошибка подключения к БД

**Проблема:** `OperationalError` или `ConnectionError`

**Решение:**
- Для SQLite: убедиться, что есть права на запись в папку
- Для PostgreSQL: проверить `DATABASE_URL` в `.env`

### 3. Ошибка токена бота

**Проблема:** `Unauthorized` или `Invalid token`

**Решение:**
- Проверить `BOT_TOKEN` в `.env`
- Убедиться, что токен правильный (получить у @BotFather)

### 4. Ошибка LLM API

**Проблема:** `APIError` или таймауты

**Решение:**
- Проверить API ключи в `.env`
- Проверить баланс API ключей
- Увеличить `LLM_TIMEOUT_SEC` в `config.py` (если нужно)

---

## 📊 Чек-лист тестирования

- [ ] Бот запускается без ошибок
- [ ] Команда `/start` работает
- [ ] Команда `/help` работает
- [ ] Команда `/stats` работает
- [ ] Команда `/rank` работает
- [ ] Команда `/case` работает
- [ ] Тренировка начинается (команда "начать")
- [ ] Кейс генерируется корректно
- [ ] Вопросы обрабатываются
- [ ] Ответы клиента генерируются
- [ ] Обратная связь работает (команда "ДА")
- [ ] Тренировка завершается корректно
- [ ] Финальный отчет генерируется
- [ ] База данных работает (данные сохраняются)
- [ ] Health check доступен на порту 8080
- [ ] Payment функционал закомментирован
- [ ] Нет ошибок в логах

---

## 🚀 Быстрая проверка одной командой

```bash
cd versions/v4.0 && \
python3 -c "
import sys
sys.path.insert(0, '.')
try:
    from bot import start_command, handle_message, stats_command
    from config import Config
    config = Config()
    print('✅ Все импорты работают')
    print(f'✅ BOT_TOKEN: {\"установлен\" if config.BOT_TOKEN else \"НЕ установлен\"}')
    print(f'✅ SCENARIO_PATH: {config.SCENARIO_PATH}')
    print('✅ Бот готов к запуску')
except Exception as e:
    print(f'❌ Ошибка: {e}')
    import traceback
    traceback.print_exc()
"
```

---

## 📝 Логирование

Бот логирует все действия в консоль. Для сохранения логов:

```bash
# Запуск с сохранением логов
python3 bot.py 2>&1 | tee bot.log

# Или в фоновом режиме
nohup python3 bot.py > bot.log 2>&1 &
```

---

## 🎯 Следующие шаги

После успешного тестирования:

1. ✅ Убедиться, что все тесты пройдены
2. ✅ Проверить, что нет критических ошибок
3. ✅ Подготовить к деплою (если нужно)
4. ✅ Обновить документацию (если были изменения)

---

**Дата создания:** 2025-11-16  
**Версия:** v4.0

