# SPIN Training Bot v4.0

Telegram-бот для тренировки навыков SPIN-продаж с использованием AI.

## 📋 Описание

SPIN Training Bot - это интерактивный тренажер для отработки техники SPIN-продаж (Situation, Problem, Implication, Need-payoff). Бот генерирует реалистичные кейсы, анализирует вопросы пользователя и предоставляет персональную обратную связь.

## 🚀 Быстрый старт

### Требования

- Python 3.11+
- PostgreSQL или SQLite
- Telegram Bot Token

### Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd "SPIN Training BOT Final/versions/v4.0"
```

2. Создайте виртуальное окружение:
```bash
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Настройте переменные окружения:
```bash
cp env.v4.example .env
# Отредактируйте .env и добавьте BOT_TOKEN и другие необходимые ключи
```

5. Запустите бота:
```bash
python bot.py
```

## 📁 Структура проекта

```
v4.0/
├── bot.py                 # Основной файл бота
├── config.py              # Конфигурация
├── database/              # Модели и репозитории БД
├── engine/                # Генерация кейсов и анализ вопросов
├── services/              # Бизнес-логика
├── modules/               # Модули (payments, active_listening)
├── scenarios/             # Конфигурации сценариев
├── scripts/               # Вспомогательные скрипты
├── infrastructure/        # Health check server
├── docs/                  # Документация
├── requirements.txt       # Зависимости
├── Dockerfile            # Docker конфигурация
└── railway.json          # Railway конфигурация
```

## 🔧 Конфигурация

Основные настройки находятся в файле `.env`:

```env
BOT_TOKEN=your_telegram_bot_token
OPENAI_API_KEY=your_openai_key
ANTHROPIC_API_KEY=your_anthropic_key
DATABASE_URL=postgresql://... или sqlite:///database.db
ADMIN_USER_IDS=123456789,987654321
```

## 📚 Документация

Полная документация находится в папке `docs/`:

- `DATABASE_ARCHITECTURE.md` - Архитектура базы данных
- `DIALOG_TREE.md` - Дерево диалогов бота
- `PERFORMANCE.md` - Оптимизация производительности
- `CHANNEL_SUBSCRIPTION_FLOW.md` - Функционал подписки на канал
- `MESSAGES_DOCUMENTATION.md` - Все текстовые сообщения
- `RAILWAY_DEPLOY_v4.md` - Инструкции по деплою
- `LOCAL_DEVELOPMENT_SETUP.md` - Настройка локальной разработки

## 🎯 Основные команды

- `/start` - Начать новую тренировку
- `/stats` - Статистика пользователя
- `/rank` - Информация о ранге и достижениях
- `/caseinfo` - Информация о текущем кейсе
- `/help` - Справка по командам

## 🏗️ Архитектура

- **Multi-bot architecture** - Поддержка нескольких ботов с общей БД
- **Async/await** - Асинхронная работа с БД и API
- **SQLAlchemy** - ORM для работы с БД
- **PostgreSQL/SQLite** - Поддержка обеих СУБД

## 📦 Деплой

### Railway

См. `docs/RAILWAY_DEPLOY_v4.md`

### Docker

```bash
docker build -t spin-bot .
docker run -d --env-file .env spin-bot
```

## 🔒 Безопасность

- Все секреты хранятся в `.env` (не коммитится в Git)
- Проверка прав администратора для критических команд
- Валидация входных данных

## 📝 Лицензия

См. файл `LICENSE`

## 👤 Автор

Александр Готальский

---

**Версия:** 4.0  
**Дата релиза:** 2025-01-16
